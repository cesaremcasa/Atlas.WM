name: job defaults directory fixture
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  check:
    defaults:
      run:
        working-directory: /tmp/project
    runs-on: ubuntu-latest
    steps: []
